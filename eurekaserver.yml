server:
  port: 8070

eureka:
  instance:
    hostname: localhost

  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8071/eureka
      fetchRegistry: false
      registerWithEureka: false
